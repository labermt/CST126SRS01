
#include "stdafx.h"
#include <cctype>  
#include <cstddef>  
#include <cstring> 

char* strtrim(char str[])
{
	char* begin = str;
	char* end = nullptr;

	if (str == nullptr)
	{
		return end;
	}
	else
	{
		while (end != '\0' || begin != '\0')
		{
			
		}
	}


}